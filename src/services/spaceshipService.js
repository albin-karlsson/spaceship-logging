export class SpaceshipService {
  static startSpaceship(name) {
    console.log(`${name} is starting...`);
  }

  static landSpaceship(name) {
    console.log(`${name} is landing...`);
  }

  static goIntoHyperdrive(name) {
    console.log(`${name} is going into hyperdrive!`);
  }
}
